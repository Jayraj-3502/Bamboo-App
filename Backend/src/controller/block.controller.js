export async function addToBlockList(req, res) {
  // User login verification
  // Getting all the necessary details
  // checking that blocking person exist or not if not the throw error
  // check if blocker already have block schema in DB or not
  // if yes the add blocking email into blockedIds
  // check that it's update or not. If not then throw error
  // else create a block schema in DB then add email
  // check that it's created or not. If not then throw error
  // return response
}

export async function removeFromBlockList() {}
